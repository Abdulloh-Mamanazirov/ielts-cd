import type { PartialContent } from "./index";

/**
 * Russian marketing copy.
 *
 * Deliberately empty, for the same reason as `uz.ts`: the instructor's own
 * words, not a guess at them. Fill in any subset of the sections in `en.ts`;
 * anything missing falls back to English. When enough is here, add "ru" to
 * ACTIVE_LOCALES in ./index.ts.
 */
export const ru: PartialContent = {};
