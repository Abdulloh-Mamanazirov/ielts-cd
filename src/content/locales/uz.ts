import type { PartialContent } from "./index";

/**
 * Uzbek marketing copy.
 *
 * Deliberately empty. This is a public-facing page for a real instructor with a
 * named reputation, so the wording has to come from him rather than from a
 * machine translation of his own sales copy — a mistranslated band promise or
 * an awkward tagline costs him students.
 *
 * Fill in any subset of the sections in `en.ts`; anything missing falls back to
 * English. When enough is here, add "uz" to ACTIVE_LOCALES in ./index.ts.
 */
export const uz: PartialContent = {};
